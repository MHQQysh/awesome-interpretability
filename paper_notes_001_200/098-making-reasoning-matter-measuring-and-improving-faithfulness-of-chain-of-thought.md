# 098. Making Reasoning Matter: Measuring and Improving Faithfulness of Chain-of-Thought Reasoning

> 逐篇阅读记录：第 98 篇 / 200。以下内容基于论文 PDF 文本、正式元数据和该论文的摘要；方法、baseline 和 finding 的具体数值应以原文表格为最终依据。

## 0. 论文信息

- **作者**：Debjit Paul, Robert West, Antoine Bosselut, Boi Faltings
- **发表 venue / date**：EMNLP / 2024/01
- **正式页面**：[Paper](https://aclanthology.org/2024.findings-emnlp.882)
- **领域标签**：Rationale, Reasoning, Hallucination, Behavior, Analyze
- **本地 PDF 文本规模**：约 12,861 个词

## 1. Abstract 讲解

- **研究问题**：模型输出可能不事实、不忠实或无法可靠归因，研究需要把这些风险转化为可评测、可解释的对象。
- **摘要主线**：解决大模型推理过程不透明、正确性信号难以从内部状态读出的问题。。方法上以Rationale为主线，结合论文摘要中的核心设定：Large language models (LLMs) have been shown to perform better when asked to reason step-by-step before answering a question.
- **阅读解释**：摘要通常完成“现象/缺口 -> 方法 -> 实验对象 -> 结论”的压缩叙述。阅读这篇论文时，应把摘要中的 claim 拆成可验证的实验问题，而不要把摘要里的提升直接当成跨模型结论。

## 2. Introduction 讲解

- **引言结构**：1 Introduction CoT, LLMs can generate plausible explanations；3 FRODO soning problems (X) and pairs of reasoning steps；4 Experiments；4 Please note that in the reasoner module, we only consider evaluate the causal effect by providing reasoning；5 Results；7 Related Work；8 Conclusion；2021. Training verifiers to solve math word prob-
- **引言关键线索**：with the final answer not necessarily guaranteed to Chain-of-thought (CoT) reasoning techniques have follow the reasoning chain or imply a causal rela- been shown to improve the performance of large tion between the reasoning chain and the model鈥檚 language models (LLMs) by generating step-by- outcome (Lyu et al., 2023). Most recent efforts step reasoning traces before generating a final an- have either focused on the performance of LLMs swer (Wei et al., 2022). Many works suggest that on various reasoning tasks or their faithfulness in the reasoning process described in CoT explana- CoT generation, ignoring the sequential relation- tions may be a possible description of how models ship between CoT and the final answer (Huang and make predictions (Kojima et al., 2022; Yao et al., Chang, 2023; Lanham et al., 2023). 2023; Sun et al., 2023). However, despite the re- markable success of CoT i
- **缺口与贡献的读法**：重点区分作者提出的新测量、新模型、新数据集、新干预，还是把已有解释工具应用到新任务；这决定论文属于方法创新、评测创新还是应用研究。

## 3. Method / Framework 讲解

- **方法段落线索**：erate correct reasoning steps and robustly rea- son over these steps. F RODO consists of an Figure 1: An example of our proposed causal analysis inference module that learns to generate correct to measure the faithfulness of the final output to the CoT reasoning steps using an implicit causal reward generated by the model. We perturbed CoT rationales function and a reasoning module that learns to and studied the causal impact on the model鈥檚 behaviour. faithfully reason over these intermediate infer- ences using a counterfactual and causal pref- erence objective. Our experiments show that F RODO significantly outperforms four compet- traces can be incorrect (Zhang et al., 2023) and itive baselines. Furthermore, F RODO improves unfaithful (Turpin et al., 2023). the robustness and generalization ability of the Reasoning implicitly involves two steps: identi- reasoning LM, yielding higher performance on fying the rules and facts (inference 
- **方法与解释性关系**：该论文主要围绕 `Rationale, Reasoning, Hallucination, Behavior, Analyze` 展开；应追踪输入、内部状态/解释单元、干预或评分函数、最终输出之间的数据流。
- **关键检查点**：解释单元是 token、layer、attention head、MLP、neuron、SAE feature、rationale、source document 还是外部知识；不同单元不能直接横向比较。

## 4. Baseline 与对比讲解

- **检测到的 baseline / comparison 关键词**：Furthermore, RODO improves unfaithful, Turpin et al, Baselines, We perform the causal, GPT-4 considers them in-, We compare F RODO, COT in the final, App, Comparing F RODO with, We now on four, The base models, Table 4, We consider T5- and, Mistral-7B . We report, SFT + CoT and, ARIO, OOD performance
- **对比维度**：通常需要同时看任务性能、解释质量/faithfulness、计算成本、扰动后的稳定性和副作用；只看主任务分数会掩盖解释方法的代价。
- **正文对比证据索引**：
  - itive baselines. Furthermore, F RODO improves unfaithful (Turpin et al., 2023).
  - comparison between the two potential outcomes
  - Baselines. We perform the causal analysis on a tervened reasoning steps, GPT-4 considers them in-
  - We compare F RODO with four strong baselines: variation in the causal effects of COT in the final
  - the baselines are reported in App. A.4. rect effect over models trained only with language
  - Comparing F RODO with Baselines. We now on four different reasoning tasks. The base models

## 5. Experiments 与 Findings 讲解

- **可检测的数值信号**：未检测到稳定的百分比/倍数表达；请直接查看实验表格。
- **结果解读顺序**：先确认数据集、模型、prompt、评价器和预算是否与 baseline 完全一致，再判断提升来自方法本身还是协议差异。
- **正文 finding 证据索引**：
  - F RODO significantly outperforms four compet- traces can be incorrect (Zhang et al., 2023) and
  - itive baselines. Furthermore, F RODO improves unfaithful (Turpin et al., 2023).
  - out-of-distribution test sets. Finally, we find sary to reach a conclusion and then robustly using
  - them to reach said conclusion (Levesque, 1986).
  - ence chains to arrive at a conclusion.1 In standard
  - been shown to improve the performance of large tion between the reasoning chain and the model鈥檚
  - diation analysis is a method of causal inference that ond, we train another small-sized LM to improve

## 6. Conclusion、局限与可复现性

- **结论段落线索**：that F RODO鈥檚 rationales are more faithful to its them to reach said conclusion (Levesque, 1986). final answer predictions than standard super- vised fine-tuning. Our paper studies whether LLMs reliably use infer- ence chains to arrive at a conclusion.1 In standard
- **局限/未来工作线索**：Acknowledgment 9 Limitations；We would like to thank Angelika Romanou, Beat- A limitation of our Causal Analysis metric is that it
- **可复现核对表**：模型与版本、数据集切分、prompt、随机种子、baseline 实现、评价脚本、解释单元位置、干预强度、显存/时间成本。

## 7. 一句话定位

这篇论文把“Making Reasoning Matter: Measuring and Improving Faithfulness of Chain-of-Thought Reasoning”放在从行为现象/内部表征分析走向可验证解释、可控干预或可信应用的研究链条上；真正的贡献需要通过其 baseline、ablation 和跨设置 finding 共同判断。
