# 170. Rationale Behind Essay Scores: Enhancing S-LLM’s Multi-Trait Essay Scoring with Rationale Generated by LLMs

> 逐篇阅读记录：第 170 篇 / 200。以下内容基于论文 PDF 文本、正式元数据和该论文的摘要；方法、baseline 和 finding 的具体数值应以原文表格为最终依据。

## 0. 论文信息

- **作者**：SeongYeub Chu, J. Kim, Bryan Wong, Mun Yong Yi
- **发表 venue / date**：NAACL / 2025/01
- **正式页面**：[Paper](https://aclanthology.org/2025.findings-naacl.322)
- **领域标签**：Rationale, Evaluation, LLM, Behavior, Evaluate
- **本地 PDF 文本规模**：约 12,439 个词

## 1. Abstract 讲解

- **研究问题**：模型生成的理由可能只是事后合理化，研究需要同时考察理由的可读性、忠实性和对决策的实际解释力。
- **摘要主线**：解决自然语言解释或归因结果是否忠实反映模型决策机制的问题。。方法上以Rationale为主线，结合论文摘要中的核心设定：Existing automated essay scoring (AES) has solely relied on essay text without using explanatory rationales for the scores, thereby forgoing an opportunity to capture the specific aspects evaluated by rubric indicators i
- **阅读解释**：摘要通常完成“现象/缺口 -> 方法 -> 实验对象 -> 结论”的压缩叙述。阅读这篇论文时，应把摘要中的 claim 拆成可验证的实验问题，而不要把摘要里的提升直接当成跨模型结论。

## 2. Introduction 讲解

- **引言结构**：1 Introduction；3 RMTS In the current study, we utilize various pre-trained；6 Conclusion；2022. On the use of bert for automated essay scor-；2020. Measuring association between labels and free-；0125. When this study was conducted, the cost for
- **引言关键线索**：tifies relevant essay sections that demonstrate spe- Multi-trait essay scoring, which evaluates essays cific traits and links them directly to the rubric. on multiple dimensions such as Content, Organi- This approach mirrors how human evaluators use zation, and Style, rather than on a single holistic rubrics to assess essays in real-world settings (Free- score, has recently become a central issue in auto- man and Miller, 2001). For instance, a rationale for mated essay scoring (AES). Extensive research in Organization highlights transitions and structure, this area has primarily utilized BERT and trait-wise leading to more precise, rubric-aligned evaluations. layers to predict scores for individual traits (Math- Without rationales, the model may overlook key el- ias and Bhattacharyya, 2020; Ridley et al., 2021; ements and score less accurately by focusing only Kumar et al., 2021; Do et a
- **缺口与贡献的读法**：重点区分作者提出的新测量、新模型、新数据集、新干预，还是把已有解释工具应用到新任务；这决定论文属于方法创新、评测创新还是应用研究。

## 3. Method / Framework 讲解

- **方法段落线索**：say scoring that integrates prompt-engineering- based large language models (LLMs) with a fine-tuning-based essay scoring model using a smaller large language model (S-LLM). RMTS Figure 1: Unlike existing methods (A), we use multi- uses an LLM-based trait-wise rationale genera- ple prompt-engineering LLMs to generate trait-specific tion system where a separate LLM agent gen- rationales based on rubric guidelines as shown in (B), erates trait-specific rationales based on rubric which are then combined with an S-LLM for compre- guidelines, which the scoring model uses to hensive evaluation. accurately predict multi-trait scores. Exten- sive experiments on benchmark datasets, in- cluding ASAP, ASAP++, and Feedback Prize, for greater computational efficiency. Despite these show that RMTS significantly outperforms efforts, most studies have used essay texts alone state-of-the-art models and vanilla S-LLMs in to predict labels as represented 
- **方法与解释性关系**：该论文主要围绕 `Rationale, Evaluation, LLM, Behavior, Evaluate` 展开；应追踪输入、内部状态/解释单元、干预或评分函数、最终输出之间的数据流。
- **关键检查点**：解释单元是 token、layer、attention head、MLP、neuron、SAE feature、rationale、source document 还是外部知识；不同单元不能直接横向比较。

## 4. Baseline 与对比讲解

- **检测到的 baseline / comparison 关键词**：ASAP and scores and, However, Baselines measures score disparities, We also included baseline, For a fair comparison, Do et al, Most models performed at, Figure 5, Performance comparison of S-LLMs, Performance Comparison, RQ2, MTL-BiLSTM has a higher, Overall score, RMTS against the baseline
- **对比维度**：通常需要同时看任务性能、解释质量/faithfulness、计算成本、扰动后的稳定性和副作用；只看主任务分数会掩盖解释方法的代价。
- **正文对比证据索引**：
  - baselines in essay scoring on the ASAP and scores and rationales. However, this approach did
  - 4.3 Baselines measures score disparities between human raters
  - text generation tasks. We also included baseline
  - on each baseline). For a fair comparison with the
  - we used the performance data of four baseline
  - (Do et al., 2024), along with two additional baseline

## 5. Experiments 与 Findings 讲解

- **可检测的数值信号**：未检测到稳定的百分比/倍数表达；请直接查看实验表格。
- **结果解读顺序**：先确认数据集、模型、prompt、评价器和预算是否与 baseline 完全一致，再判断提升来自方法本身还是协议差异。
- **正文 finding 证据索引**：
  - show that RMTS significantly outperforms efforts, most studies have used essay texts alone
  - responses rather than multiple trait scores of long- (Devlin et al., 2018) significantly improved AES
  - generated rationales significantly improves erate results. Another study (Li et al., 2023) used
  - ASAP++ benchmark datasets. not outperform fine-tuned models like BERT (De-
  - the S-LLM. This dual-process approach improves
  - As illustrated in Figure 2, individual trait-specific the essay text to improve model inference. Special
  - tionales improve the reliability of multi-trait 5

## 6. Conclusion、局限与可复现性

- **结论段落线索**：since they appear in fewer prompts. We used T5, Flan-T5, and BART in the experiments, fine-tuning This paper introduces RMTS, a framework that each model by removing one trait at a time. uses prompt-engineering-based LLMs to improve A consistent decline in performance is observed multi-trait essay scoring in S-LLMs by generating across all traits when the corresponding rationale trait-specific rationales aligned with rubric guide- is removed, confirming that trait rationales signifi- lines and incorporating them into the scoring pro- cantly influence their respective assessments. For cess. Our results show that RMTS with S-LLMs instance, removing the rationale for Conventions significantly improves the performance of each results in a performance drop for that trait, particu- vanilla model, with RMTS using T5 even outper- larly when compared to RMTS, which utilizes ra- forming state-of-the-art baselines. Additionally, re- 5818 moving rationales negatively impacts performance. scoring. 
- **局限/未来工作线索**：Limitations agreement provision for scaled disagreement or par-
- **可复现核对表**：模型与版本、数据集切分、prompt、随机种子、baseline 实现、评价脚本、解释单元位置、干预强度、显存/时间成本。

## 7. 一句话定位

这篇论文把“Rationale Behind Essay Scores: Enhancing S-LLM’s Multi-Trait Essay Scoring with Rationale Generated by LLMs”放在从行为现象/内部表征分析走向可验证解释、可控干预或可信应用的研究链条上；真正的贡献需要通过其 baseline、ablation 和跨设置 finding 共同判断。
